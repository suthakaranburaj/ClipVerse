import { create } from "zustand";

// helper function 1 create localstore user 2 delete 

const useStore = create((set) => ({
    // user : null 
    // login : () => { axios->login->response data->user }
}));

export default useStore;
